# Claude Instructions

- Do not ask clarifying questions before implementing. Make a reasonable decision and build it.
- Do not ask for confirmation before proceeding with tasks.
- Match the existing code style exactly (4-space indent, named arrow handlers, inline prop types, no paren-wrapped single-element returns).
- Stay within the `app/` folder — no API routes or outside-app changes.
